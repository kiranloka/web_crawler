Spider Crawl 🕷️
Interactive Rust CLI Web Crawler

Spider Crawl is a simple, interactive web crawler built in Rust. It runs as a CLI tool: you start it, enter a URL, and watch it crawl pages and print structured results directly in your terminal.

Built to explore async networking, HTML parsing, and crawler architecture in a lightweight, developer‑friendly way.

